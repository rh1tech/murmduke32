/*
 * FreeRTOS Compatibility Header for RP2350
 * Duke3D ESP32 port uses FreeRTOS, but we don't need it on RP2350
 */
#ifndef FREERTOS_FREERTOS_H
#define FREERTOS_FREERTOS_H

// Empty header - FreeRTOS not used on RP2350

#endif /* FREERTOS_FREERTOS_H */
